delimiter $$
create trigger t1 
before update 
on student
for each row 
begin
insert into backup_student values(old.id,old.name,old.marks,old.department,old.subject,old.Grants,old.email);
end;
$$

delimiter $$
create trigger t2 
after update  
on student
for each row 
begin
insert into backup_student values(old.id,old.name,old.marks,old.department,old.subject,old.Grants,old.email);
end;
$$

delimiter @@
create trigger t3
before insert 
on student 
for each row
begin 
set new.name = upper(new.name);
end;
@@

create table bkup(oname varchar(20),nname varchar(20),omarks int,nmarks int);
delimiter $$
create trigger t4
after update 
on student
for each row 
begin 
insert into bkup values(old.name,new.name,old.marks,new.marks);***********note here old.oname is wrong we have to write the column name as per the student table
end;
$$ 
 